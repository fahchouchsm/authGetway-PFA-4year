server:
  port: 8082

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3310/smartcity}
    username: ${SPRING_DATASOURCE_USERNAME:auth}
    password: ${SPRING_DATASOURCE_PASSWORD:auth}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  cloud:
    gateway:
      mvc:
        routes:
          - id: dotnet-service
            uri: http://localhost:5181
              - Path=/api/city/**

  data:
    redis:
      host: localhost
      port: 6379
  mail:
    host: smtp-relay.brevo.com
    port: 587
    username: a2668c001@smtp-brevo.com
    password: sFSYxwj8AEZWO610
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  frontend-url: http://localhost:3000
  mail:
    from: simofff0@gmail.com
  use-https: false
  jwt-secret: "dGhpcyBpcyBhIGRldiBzZWNyZXQga2V5IGZvciBkZXZlbG9wbWVudA=="
  jwt-refresh-secret: "dGhpcyBpcyBhIGRldmVsb3BtZW50IHJlZnJlc2gga2V5IGZvciBkZXY="

springdoc:
  swagger-ui:
    urls:
      - name: jobfinder
        url: /jobs/v3/api-docs
      - name: eventhandler
        url: /events/v3/api-docs
  api-docs:
    enabled: true