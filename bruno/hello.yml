info:
  name: hello
  type: http
  seq: 3

http:
  method: GET
  url: "{{ip}}/user/hello"
  params:
    - name: ""
      value: ""
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
