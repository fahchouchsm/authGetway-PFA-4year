info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: "{{ip}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "fahchouchsm@gmail.com",
        "password": "StrongPass123!",
        "stayLogin": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
